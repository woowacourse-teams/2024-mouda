{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true, // 파일을 생성하지 않음
    "isolatedModules": true // 모듈을 개별적으로 처리
  },
  "include": ["src/**/*.ts", "src/**/*.tsx", "jest.setup.ts"],
  "exclude": ["node_modules", "dist"]
}
