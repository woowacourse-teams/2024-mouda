spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

oauth:
  kakao:
    redirect-uri: https://dev.mouda.site/kakao-o-auth
