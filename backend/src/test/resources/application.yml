spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    ddl-auto: create-drop
    defer-datasource-initialization: true
  sql:
    init:
      data-locations: classpath:test-data.sql


datetime:
  format:
    date: yyyy-MM-dd
    time: HH:mm
